To install node.js dependencies, run ```npm install```.

To test run the server:
1. run ```node server.js```
2. Open any browser and type: http://localhost:8080
3. Then type: http://localhost:8080/inquiries